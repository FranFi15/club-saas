/**
 * Native: the real PagerView. The `.web.js` sibling replaces it on web, where
 * react-native-pager-view cannot bundle (it imports react-native internals).
 */
export { default } from 'react-native-pager-view';
